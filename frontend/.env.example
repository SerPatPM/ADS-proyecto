# ── Frontend EPiC Playground ─────────────────────────────────────────────────
# Copia este archivo como .env.local para desarrollo local.
# En Railway estas variables se configuran en el panel de la plataforma.

# URL del backend (sin barra al final)
# Desarrollo local:
VITE_API_URL=http://localhost:8000
# Producción (reemplaza con la URL real de tu servicio backend en Railway):
# VITE_API_URL=https://epic-backend.up.railway.app
